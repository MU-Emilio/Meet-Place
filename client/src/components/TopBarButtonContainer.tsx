import React from "react";

const styles = {
  topBarButtons: {
    width: "300px",
    height: "100px",
  },
};

const TopBarButtonContainer = () => {
  return (
    <div style={styles.topBarButtons} className="bg-green-50">
      TopBarButtonContainer
    </div>
  );
};

export default TopBarButtonContainer;
